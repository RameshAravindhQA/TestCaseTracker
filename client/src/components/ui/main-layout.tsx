import React from 'react';
import { MainLayout as BaseMainLayout } from '../layout/main-layout';

// Re-export the main layout component for backward compatibility
export const MainLayout = BaseMainLayout;
export default MainLayout;